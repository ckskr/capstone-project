import StyledButton from './StyledButton';

export default {
	title: 'Components/StyledButton',
	component: StyledButton,
};

export const Add = () => <StyledButton variant="add" />;

export const Delete = () => <StyledButton variant="delete" />;
